{
  "embeds": [
    {
      "title": "New Punishment :timer:",
      "description": "The player `${entry.name}` was ip-banned by `${entry.executorName}` for `${entry.reason}` ${entry.duration}",
      "color": "16711680",
      "footer": {
        "text": "Powered by LiteBans Webhook"
      }
    }
  ]
}
