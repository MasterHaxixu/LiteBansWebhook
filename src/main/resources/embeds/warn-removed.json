{
  "embeds": [
    {
      "title": "Warning Revoked :white_check_mark:",
      "description": "The player `${entry.name}` was unwarned by `${entry.executorName}` for `${entry.reason}`",
      "color": "3329330",
      "footer": {
        "text": "${entry.serverOrigin} | Powered by LiteBans Webhook"
      }
    }
  ]
}
